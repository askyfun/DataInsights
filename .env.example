# Data Insights 环境变量（前后端共用一份，仓库根目录）
#
# 变量名**不加前缀**：后端进程是唯一读者，没有命名空间要抢；而 DATABASE_URL /
# PORT 是 12-factor 的标准名 —— Heroku / Railway / Render / Fly 等平台自动注入的
# 就是裸名，加前缀反而逼你手工映射一遍。唯一例外是前端变量必须带 VITE_ 前缀
# （Vite 的硬性要求，不是风格选择）。
#
# ⚠️ docker compose 会读本文件**做变量插值**（见 docker-compose.allinone.yml 里的 ${...}），
# 只有被 environment: 逐个 ${...} 引用的变量才会进容器 —— 本文件不会被整体注入容器。
# 别把无关工具的变量混进这个文件里。
#
# 配置只有一个来源 —— 环境变量。项目刻意不提供任何配置文件（没有 config.toml / config.yaml），
# 需要你填的值全在这里。优先级（业界通用规则，12-factor / dotenv 惯例）：
#
#     真实系统环境变量  >  本文件(.env)  >  内置默认值
#
# 即：docker run --env-file / docker compose environment / K8s env / shell export
# 传入的同名变量永远覆盖本文件；本文件只承担本地开发的便利作用。
#
# 用法：
#     本地开发   cp .env.example .env   然后按需修改
#     Docker     docker run --env-file .env ...
#
# 格式约束（为了同时兼容 docker --env-file，docker 的解析器比 dotenv 严格）：
#     只写 KEY=value；不要加引号、不要用 export、不要在值里写注释用的 # 字符。

# ============================================================
# 常用配置（就这四类，其余全有合理默认值）
# ============================================================

# PostgreSQL 连接串。**唯一必填项**，缺省则启动直接失败。
# 注意：主机名要用目标网络内可达的名字（本机开发用 localhost，compose 用服务名 postgres）。
DATABASE_URL=postgres://postgres:postgres@localhost:5432/insight_dev?sslmode=disable

# HTTP 监听端口，默认 23352。
# 监听地址固定 0.0.0.0，**不提供**环境变量覆盖（容器场景下它本就是唯一合理值）。
PORT=23352

# Sentry DSN（后端项目）。留空 = 关闭上报。
SENTRY_DSN=

# Sentry DSN（前端项目）。留空 = 完全跳过 Sentry 初始化，不打点也不上报。
# 与后端是两个独立 Sentry 项目，值通常不同。
# ⚠️ VITE_* 在「构建期」被内联进 JS 产物，改完必须重新 build 才生效，绝不能放密钥。
VITE_SENTRY_DSN=

# ============================================================
# 进阶选项（默认全部无需配置，需要时取消注释）
# ============================================================

# CORS：默认放开所有来源（平台 API 无登录态，CORS 不构成安全边界）。
# 将来引入认证或需要限定域名时，逗号分隔填白名单。
# CORS_ALLOWED_ORIGINS=https://app.example.com

# 前后端分开部署：把 API 的绝对地址烧进前端产物。
# 默认无需配置 —— 生产构建自动同源，开发回退 http://<当前访问主机名>:23352。
# VITE_API_BASE_URL=http://localhost:23352

# 数据源密码 AES-256 加密密钥，64 位 hex（openssl rand -hex 32）。
# 留空 = 由系统在首次启动时随机生成并持久化到数据库 bi_setting 表，
#      此后每次启动都复用同一把，保证已加密的密码可解。显式配置时环境变量优先。
# 生产部署强烈建议显式设置。
# SECURITY_KEY=

# 前端构建产物目录：设置后本进程一并托管页面（未命中文件回落 index.html）。
# 留空 = 只提供 API（本地开发默认形态，页面走 Vite dev server）。
# 单镜像容器里由镜像预设为 /app/web，本地验证单进程形态用 make serve（已内联该变量）。
# STATIC_DIR=frontend/dist
